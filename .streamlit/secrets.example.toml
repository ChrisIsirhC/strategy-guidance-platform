# Copy this file to .streamlit/secrets.toml for local testing.
ADMIN_USERNAME = "admin"
ADMIN_PASSWORD = "REPLACE_WITH_A_PRIVATE_PASSWORD"

# Production: add these only after creating the Supabase project.
# SUPABASE_URL = "https://YOUR_PROJECT.supabase.co"
# 新项目优先使用服务端 Secret Key（不要放到前端，也不要提交 GitHub）。
# SUPABASE_SECRET_KEY = "YOUR_SERVER_SIDE_SECRET_KEY"
# 旧项目仍可使用下面的兼容名称：
# SUPABASE_SERVICE_ROLE_KEY = "YOUR_SERVER_SIDE_SERVICE_ROLE_KEY"
