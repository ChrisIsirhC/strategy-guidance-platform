# 数据更新管理

双击 `启动策略指引平台.bat` 打开当前 Streamlit 网站（8511 端口），同时在后台启动本地日表同步服务（4174 端口，不会打开旧站）。单独运行 `streamlit run app.py` 或 `python -m http.server` 都不会启动本地自动抓表服务。

- 每天 08:00 与 18:00 自动检查腾讯共享表。
- 启动同步服务及访问其旧首页、历史页时，也会根据当前时段补做一次检查；因此即使服务没有在整点运行，下一次通过 BAT 启动仍会补查。访问 Streamlit 站点本身不会触发抓表。
- 同一时段只执行一次：18:00 前按当天的 `08` 时段判定，18:00 起按当天的 `18` 时段判定。
- 每次检查会复核最近已归档日期，并扫描其后的所有日表；任何尚未归档的中间日期也会补档。已归档日表正文变化会保留新快照，再重建网站数据包并原子替换；未变更则不重建。
- 更新过程使用进程内锁和文件锁，避免多位访问者重复抓取。异常退出遗留的文件锁超过两小时会自动恢复。
- 同步完成后刷新 Streamlit 页面可读取新的本地数据；Streamlit 的右上角展示已发布的云端同步状态，不是本地同步服务的实时进度。失败时继续显示上一次成功发布的数据。

服务需要保持运行，才能在 08:00 和 18:00 准点主动检查；若服务关闭，下一次启动或访问会执行对应时段的补查。
